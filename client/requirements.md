## Packages
framer-motion | Smooth animations and page transitions
recharts | Admin dashboard analytics charts
clsx | Utility for conditional class names (often used with tailwind-merge)
tailwind-merge | Utility for merging tailwind classes

## Notes
Backend API uses /api prefix.
Authentication uses HTTP-only cookies (credentials: "include").
Images are handled via multipart/form-data upload to /api/listings/:id/images.
Google Maps link is a simple text field for now.
